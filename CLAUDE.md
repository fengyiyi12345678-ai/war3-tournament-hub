# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**war3-tournament-hub** — a tournament hub for Warcraft III: The Frozen Throne.

## Current state

This is a greenfield repository. As of this writing it contains only `README.md`
and no source code, build tooling, tests, or dependency manifests. There is no
build/lint/test command to document yet.

When you add the first real code, update this file with:
- The build, lint, and test commands (including how to run a single test).
- The tech stack and project layout once it exists.
- The high-level architecture — the cross-file "big picture" that isn't obvious
  from reading any single file.

Do not document conventions or commands that aren't actually present in the repo.
